"""
Hexagonal Spiral Parser
"""

import csv
#declare global variables


def reader(name, sheetVert): #read intermediate output file
	with open(name+"_intermediate.csv",'r') as plotlist:
		plot_reader = 
	
#return array
def spiral_array():
	return spiral